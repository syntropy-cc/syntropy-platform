/**
 * User management placeholder (COMP-032.6).
 * Backend list/roles API not yet implemented; UI shell only.
 */

export default function AdminUsersPage() {
  return (
    <div>
      <h1>User management</h1>
      <p>User management (list and role assignment) is coming soon. The backend will expose an admin-only users API.</p>
    </div>
  );
}
