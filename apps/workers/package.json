{
  "name": "@syntropy/workers",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsc && node dist/main.js",
    "lint": "echo 'lint placeholder'",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@syntropy/platform-core": "workspace:*",
    "prom-client": "^15.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "~5.6.0",
    "vitest": "^2.1.0"
  }
}
