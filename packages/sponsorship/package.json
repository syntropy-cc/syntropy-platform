{
  "name": "@syntropy/sponsorship",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "echo 'lint placeholder'",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@syntropy/event-bus": "workspace:*",
    "@syntropy/platform-core": "workspace:*",
    "@syntropy/types": "workspace:*",
    "pg": "^8.13.0",
    "stripe": "^17.0.0"
  },
  "devDependencies": {
    "@testcontainers/postgresql": "^10.14.0",
    "@types/node": "^22.10.0",
    "@types/pg": "^8.11.0",
    "typescript": "~5.6.0",
    "vitest": "^2.1.0"
  }
}
